name: test_plugin
channels: 
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - psi4 >= 1.9.1
  - pycifrw

    # Testing
  - pytest
  - pytest-cov
  - mdanalysis
  - optax
  - jaxopt
  - networkx
  - jax=0.5.0
  - jaxlib=0.5.0
    # Pip-only installs
  #- pip:
  #  - codecov
